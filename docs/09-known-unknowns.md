# Known Unknowns

These are the things to verify during the upcoming Shape Reth work instead of pretending they are settled.

- exact runtime version pair most reliable for current Shape mainnet
- whether built-in chain support is enough or custom chain spec is still mandatory
- the cleanest sync mode for a usable personal RPC outcome
- whether the uploaded Reth data path will converge faster than from-genesis alternatives
- exact cutover criteria the user is happiest with
- which remaining failure classes are upstream `op-reth` issues versus Shape-specific config issues
